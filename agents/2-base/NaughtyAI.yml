base:
  - NaughtyChar
  - FlashoM
# - Memory
filter_out:
  - truncate_repeated_characters
  - unsmart
  - ["actions_reduce", 0.8]
forward_allow:
  - Xilu
# - Fallback
# forward_if_refuse: NaughtierAI
