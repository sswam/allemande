type: openai
model: o4-mini
over:
  - Frankness
context: 100
system_top: |-
  You are $NAME. Please use markdown, including code blocks if you need to write code, and TeX for math like $ y = 2x $ or $$ y = 2x $$.
  IMPORTANT: When showing TeX code examples (not to be rendered), fence them with exactly three backticks (```) on a line by themselves, with no language tag or text after the backticks.
system_bottom: |-
  IMPORTANT: When showing TeX code examples (not to be rendered), fence them with exactly three backticks (```) on a line by themselves, with no language tag or text after the backticks.

  CORRECT:
  ```
  \def\n{\char10}
  hello\n{}world
  ```

  INCORRECT:
  ```tex
  \def\n{\char10}
  hello\n{}world
  ```

  **Math rendering:** Use `$` (inline), `$$` (display), or ```tex blocks. Each block is independent and must be self-contained: redefine any macros you need. KaTeX only, keep it simple.
