#!/bin/bash -eu

## windows:
# - add windows user
# - set password and pin
# - set background
# - windows terminal
# - WSL debian
# - terminal default to debian

## debian:
# - apt update
# - apt install git ssh rsync screen neovim build-essential devscripts python-is-python3 barrier
# - ssh-keygen
# - ssh-copy-id ucm.dev
# - ssh-copy-id pi.ucm.dev
# - apt lists and preferences (i.e. 99dontbreakdebian)
# 	- cd /etc/apt/preferences.d/
# 	- scp ucm.dev:$PWD/99* /tmp/
# 	- sudo mv /tmp/99dontbreakdebian .
# - apt dist-upgrade
# - clone allemande
# - git config --global user.email who@where
# - git config --global user.name "User Name"
# - git config --global pull.rebase falsudo 
# - install python3.10-distutils bogus package or whatever it is
# - apt install python3.10
# - apt install allemande deps
# - pip install allemande deps
# - install other allemande deps, eg. whisper.cpp
# - test allemande

## windows suggested:
# - remove edge shortcuts
# - chrome
# - firefox
# - misc fixed font
# - terminal use misc fixed
# - windows search -> chrome or firefox

## debian suggested:
# - install ucm-* packages
# - arcs
# - ucm-tools
# - remote access via sshd port forward
