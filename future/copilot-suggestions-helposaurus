    echo "Usage: $0 [options] [arguments]"
    echo "Options:"
    echo "  -h, --help      Show this help message and exit"
    echo "  -v, --version   Show version number and exit"
    echo "  -l, --list      List all available commands"
    echo "  -c, --command   Show command help message"
    echo "  -s, --search    Search for a command"
    echo "  -e, --edit      Edit a command"
    echo "  -d, --delete    Delete a command"
    echo "  -a, --add       Add a new command"
    echo "  -r, --random    Show a random command"
    echo "  -u, --update    Update the database"
    echo "  -f, --force     Force update the database"
    echo "  -p, --path      Show the database path"
    echo "  -o, --open      Open the database directory"
    echo "  -t, --test      Test the database"
    echo "  -i, --info      Show information about the database"
    echo "  -q, --quiet     Quiet mode"
    echo "  -n, --no-color  No color mode"
    echo "  -x, --xtrace    Show the executed commands"
    echo "  -y, --yes       Yes mode"
    echo "  -z, --no        No mode"
    echo "  -w, --wait      Wait mode"
    echo "  -g, --debug     Debug mode"
    echo "  -k, --kill      Kill mode"
    echo "  -m, --more      More mode"
    echo "  -j, --json      JSON mode"
    echo "  -b, --bash      Bash mode"
    echo "  -i, --info      Show information about the database"
    echo "  -q, --quiet     Quiet mode"
    echo "  -n, --no-color  No color mode"
    echo "  -x, --xtrace    Show the executed commands"
    echo "  -y, --yes       Yes mode"
    echo "  -z, --no        No mode"
    echo "  -w, --wait      Wait mode"
    echo "  -g, --
